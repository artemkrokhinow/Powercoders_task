Program read "raw_client_data.csv"
You can change the file by changing the variable "fileName"
At the end, the program it will update your file 
-------
For take array use "newArray"
FORMAT [{Client_ID, Full_Name, Region_Code, ISO_Date}, ...]
-------
To instal  "npm install"
